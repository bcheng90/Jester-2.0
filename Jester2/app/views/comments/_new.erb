<ul id="form">
  <%= form_for @new_comment, html: {id: "add_comment"} do |f|%>
    <li><%= f.text_field :message, id: "text"%></li>
    <%= hidden_field_tag "joke_id", joke_id %>
    <%= submit_tag "LAUGH IT OUT" %>
  <% end %>
</ul>`